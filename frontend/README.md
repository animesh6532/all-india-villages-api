# Frontend

React + Vite dashboard for searching villages, viewing API analytics, and managing API credentials.

```bash
cp .env.example .env
npm install
npm run dev
```

The app uses the backend demo credentials by default after `npm run db:seed`.
