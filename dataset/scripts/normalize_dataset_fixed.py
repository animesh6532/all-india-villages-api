from slugify import slugify&#10;import pandas as pd&#10;from pathlib import Path&#10;&#10;CLEAN_DIR = Path(__file__).resolve().parent / "cleaned"&#10;&#10;# Load and enhance existing normalized data&#10;states = pd.read_csv(CLEAN_DIR / "states.csv")&#10;states["slug"] = states["name"].apply(slugify)&#10;states["census_code"] = states["id"].astype(str).str.zfill(2)  # Generate placeholder&#10;states.to_csv(CLEAN_DIR / "states.csv", index=False)&#10;&#10;districts = pd.read_csv(CLEAN_DIR / "districts.csv")&#10;districts["slug"] = districts["name"].apply(slugify)&#10;districts["census_code"] = districts["id"].astype(str).str.zfill(3)&#10;districts.to_csv(CLEAN_DIR / "districts.csv", index=False)&#10;&#10;# Similar for subdistricts, villages with population placeholder 0&#10;# Run after normalize_dataset.py to add missing fields&#10;print("Enhanced CSVs with slug/census_code/population placeholders")
